{
	"core.port": {
		"default": null,
		"format": "port",
		"env": "PORT"
	},
	"redis": {
		"default": "redis://localhost:6379",
		"format": "url",
		"env": "REDISTOGO_URL"
	},
	"mongo": {
		"default": "mongodb://localhost/mongodb",
		"format": "url",
		"env": "MONGOHQ_URL"
	},
	"core.session.secret": {
		"default": null,
		"format": "*",
		"env": "SESSION_SECRET"
	},
	"core.cookie.secret": {
		"default": null,
		"format": "*",
		"env": "COOKIE_SECRET"
	},
	"auth.passport.github.client_id": {
		"default": null,
		"format": "*",
		"env": "PASSPORT_GITHUB_CLIENT_ID"
	},
	"auth.passport.github.client_secret": {
		"default": null,
		"format": "*",
		"env": "PASSPORT_GITHUB_CLIENT_SECRET"
	},
	"auth.passport.imgur.client_id": {
		"default": null,
		"format": "*",
		"env": "PASSPORT_IMGUR_CLIENT_ID"
	},
	"auth.passport.imgur.client_secret": {
		"default": null,
		"format": "*",
		"env": "PASSPORT_IMGUR_CLIENT_SECRET"
	},
	"auth.passport.google.client_id": {
		"default": null,
		"format": "*",
		"env": "PASSPORT_GOOGLE_CLIENT_ID"
	},
	"auth.passport.google.client_secret": {
		"default": null,
		"format": "*",
		"env": "PASSPORT_GOOGLE_CLIENT_SECRET"
	}
}
