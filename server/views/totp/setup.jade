extends ../layout

block content
	h1 Key
	= encodedKey
	h1 QR Code
	img(src=image)
	h2 Save shizzlewizzle
	form(method='POST', action='/totp/setup')
		input(type='hidden', name='key', value=key)
		input(type='hidden', name='period', value=period)
		input(type='text', name='code')
		button Save
